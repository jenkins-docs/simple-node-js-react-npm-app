{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": ">16.0.0",
    "react-dom": ">16.0.0",
    "react-scripts": ">1.0.14"
  },
  "scripts": {
    "test-ci": "npm install && npm run build && CI=true npm run test",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
