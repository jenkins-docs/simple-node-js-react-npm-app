language: node_js
node_js:
  - "12.18.3"

install: npm ci

jobs:
  include:
    - stage: "build & test"
      name: "build application"
      script: npm run build
    - name: "run test"
      script: npm run test